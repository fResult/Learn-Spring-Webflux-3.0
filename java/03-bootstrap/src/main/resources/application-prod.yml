spring:
  application:
    name: 03 BootStrap Java (Production)

  datasource:
    url: jdbc:h2:mem:rsb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
    username: sa
    password:
    driver-class-name: org.h2.Driver
